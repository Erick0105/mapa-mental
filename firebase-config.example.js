/* Modelo do firebaseConfig — veja "Colaboração em tempo real" no README.
   Copie este arquivo para "firebase-config.js" (mesma pasta) e substitua os
   valores abaixo pelos do seu projeto Firebase. O "firebase-config.js" real
   fica de fora do git (.gitignore), então as credenciais não são
   versionadas nem sobem para o repositório. */
const firebaseConfig = {
  apiKey: "SUA_API_KEY",
  authDomain: "SEU_PROJETO.firebaseapp.com",
  databaseURL: "https://SEU_PROJETO-default-rtdb.firebaseio.com",
  projectId: "SEU_PROJETO",
};
